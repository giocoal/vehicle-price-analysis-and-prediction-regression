---
title: "car"
author: "Carbone Giorgio, Scuri Gianluca, Gazzola Michele"
date: "12/11/2021"
output:
  html_document: default
  pdf_document: default
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown


CIAO SONO GIANLUCA
This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```


# Importazione librerie
```{r importazione librerie, include=FALSE}
library(readr)
data_path <- "https://raw.githubusercontent.com/giocoal/Progetto_FoPaS/main/Car%20details%20v3.csv?token=ATGDIWTFWBAYMSALU2PY2NDBSLKYO"
```

# Lettura Dataset
```{r importazione }

car <- read_csv(data_path)
View(car)
```

# Visione dataset
``` {r lettura}
dim(car)
names(car)
str(car)

```

# Missing Value
``` {r Missing Values}
for (i in names(car)) {
  print(i)
  print(sum(is.na(car[i])))
}
```

# Missing Value
```{r missing value}
#sum(is.na(car$))
```